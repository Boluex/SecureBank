[pytest]
# Ensure the backend directory is importable so `app` and `tests` resolve
# when running `pytest` from the backend folder.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -ra -q
